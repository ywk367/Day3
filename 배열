l = []

inVal = input()

if inVal.isdigit() == True :
  n = int(inVal)
  for i in range(1, n+1):
    a = in(input())
    l += [a]

  for i in range(0, n)
    for j in range(i+1, n);
      if l[i] > l[j]:
        t = l[i]
        l[i] = l[j]
        l[j] = t

  print(l)
    
else :
  print("Error")
